apiVersion: 1

datasources:
  - name: InfluxDB3
    type: influxdb
    access: proxy
    isDefault: true
    url: http://influxdb3:8181

    jsonData:
      version: SQL
      httpMode: POST
      dbName: system_metrics
      insecureGrpc: true
    secureJsonData:
      token: "${INFLUX_TOKEN}"